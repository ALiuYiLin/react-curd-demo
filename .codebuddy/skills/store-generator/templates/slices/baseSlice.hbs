import type { StateCreator } from 'zustand'
// import { {{EntityName}}Api } from '@/api'
// import { message } from 'antd'
import type { Base, UI } from '../types'

export const createBaseSlice: StateCreator<Base & UI, [], [], Base> = (set, get) => ({
{{#each baseStates}}
  {{{name}}}: {{{defaultValue}}},
{{/each}}

{{#each baseActions}}
  {{{name}}}: async ({{#each params}}{{{name}}}{{#unless @last}}, {{/unless}}{{/each}}) => {
    // TODO: 实现 {{{name}}}{{#if description}} - {{{description}}}{{/if}}
  },
{{/each}}
})
